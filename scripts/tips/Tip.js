export const Tip = (tipObj) => {
    return `
        <section class="tip card">
            <div class="tip__topic">${Tip.topic}</div>
            <div class="tip__text">${Tip.text}</div>
        </section>
    `
}